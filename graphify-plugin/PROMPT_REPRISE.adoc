= PROMPT_REPRISE — Graphify Gradle — Session 029
:session: 029
:date: 2026-09-17

== Session 028 terminée — GF-SCHEMA-5 `schemaVersion` + tolérance consommateurs

*EPIC GF-SCHEMA est désormais COMPLET 5/5 US.* Le schéma est explicite,
tolérant, versionné, et l'incrémentalité est opt-in.

* GF-SV-0 cadrage — audit 4 constats, 7 décisions. Doc
  `.agents/CADRAGE_EPIC_GF_SCHEMA_VERSION.adoc`.
* GF-SV-1/2 graphify N0 — `@JsonIgnoreProperties(ignoreUnknown = true)` sur les
  4 data classes + `schemaVersion: Int = SCHEMA_VERSION (= 1)`. Le producteur
  émet le champ ; garde de forme 3 → 4 champs ; BDD `schema_version.feature`
  (6 scénarios) ; README 10 × 2 synchronisés.
* GF-SV-3 — `graphify-plugin:0.0.5` publié Central (bundle `895d2e53`).
  Non-régression prouvée : bakery lens 144/0, codebase graph 37/0.
* codebase committé/poussé `0b614a1` (bump 0.0.5 + 2 tests de tolérance).

Gates : 149 unit + 9 functional + 20 cucumber, 0 FAIL. `check --rerun-tasks` +
`publishToMavenLocal` VERTS. Zéro secret.

== COLLISION DE SESSION — à connaître en ouverture

Une *seconde session opencode* a travaillé en parallèle dans `workspace-bom` et
`bakery-gradle` pendant S-028. Conséquences :

* `workspace-bom` : working tree porté à `0.0.46` (avec `graphify-plugin =
  0.0.5` + `codex-plugin 0.0.9`) par l'autre session. **Non publié** (repo1 404).
* `workspace-bom/catalog:0.0.45` (publié par l'autre session, `01:21 GMT`)
  référence `graphify-plugin = "0.0.4"` — *pas* 0.0.5.
* `bakery-gradle` : 5 fichiers de l'autre session + nos 2 fichiers de bump
  0.0.5 — **non committé** (cédé à l'autre session).
* Pin actuel graphify : `workspace-catalog:0.0.45`. Sur *cette machine* il
  résout 0.0.5 (mavenLocal écrasé par notre `publishToMavenLocal`) ; sur une
  machine propre il résoudrait 0.0.4 (Central). `graphify:0.0.5` est publié mais
  référencé par aucun catalogue publié.

== Mission session 029 — COORDINATION (bloquant)

*Dès que l'autre session publie `workspace-bom/catalog:0.0.46`* :

. Bumper `settings.gradle.kts` : `workspace-catalog:0.0.45 → 0.0.46`.
. Réaligner `GraphifyPluginPublicationTest` (pin + message d'échec) sur 0.0.46.
. Vérifier `./gradlew properties` → `version: 0.0.5` résolue depuis Central.
. Puis `check --rerun-tasks` + `publishToMavenLocal`.

*Ne pas publier MEMPHIS soi-même* — 0.0.45 est pris, 0.0.46 appartient à
l'autre session. Toute ré-invocation nmcp est rejetée ou collisionne.

Puis, à la demande pilote :
1. *Adoption bakery* — committer les 2 fichiers (bump 0.0.5 + 2 tests), si
   l'autre session ne l'a pas fait.
2. *EPIC correctif garde D5* — `GraphifyPluginPublicationTest` lit le *working
   tree* du repo voisin (`../workspace-bom/gradle/libs.versions.toml`), donc
   racy entre sessions. Lire le catalogue publié, pas le working tree.

== Rappel

- EPIC GF-SCHEMA COMPLET 5/5 ; EPIC GF-FINGERPRINT COMPLET 6/6.
- Borough N0, tooling 0.0.5 Central.
- Baby-step DDD/TDD/BDD strict, 3 niveaux (unit → functional → cucumber).
- Filtre `graphify.steps.**` obligatoire sur le task `test`.
- Le source set `functionalTest` n'a PAS `main` sur son classpath de
  compilation — les tests de modèle vont dans `test/`.
- Version propre dérivée du catalogue : tout bump passe par MEMPHIS.
