= PROMPT_REPRISE — Graphify Gradle — Session 028
:session: 028
:date: 2026-09-17

== Session 027 terminée — Publication Central graphify-plugin 0.0.4

Ordre pilote acté : 2 (S-026) → *3 (fait)* → 1.

* Contrainte découverte : la version propre du borough est *dérivée du
  catalogue workspace* (`version = ws.versions.graphify.plugin.get()`), donc
  publier 0.0.4 exigeait d'abord un bump MEMPHIS (RELEASE_PROTOCOL).
* *Baseline MEMPHIS rouge à HEAD réparée* : `PlatformCatalogAlignmentTest`
  épinglait `workspace-bom 0.0.36` (vs toml 0.0.43) et `i18n-contracts 0.0.2`
  (vs toml 0.0.4, dérive S-217 LANG-22). Réalignées (piège #10).
* MEMPHIS : toml `workspace-bom 0.0.43→0.0.44` + `graphify-plugin 0.0.3→0.0.4`,
  gardes réparées, `contracts.catalog.*` VERT, mavenLocal 0.0.44 VERT.
* graphify : pin `workspace-catalog 0.0.33→0.0.44`, toml local `graphify 0.0.4`,
  garde D5 réalignée, `version: 0.0.4` résolue,
  `check --rerun-tasks` VERT (165 tests 0 FAIL).
* Central : `graphify-plugin:0.0.4` (bundle `96f87d32`) +
  `workspace-bom/catalog:0.0.44` (bundle `dbe3ef8e`), repo1 HTTP 200 en ~7 min.
* Consommateurs : aucun impact (bakery pinné 0.0.43, adoptions opt-in D3).

== Prochaine Session (028) — GF-SCHEMA-5 `schemaVersion` (P2, cross-borough)

*Restant du seul EPIC inachevé* (GF-SCHEMA 4/5 US). Chantier multi-borough.

* Contenu : ajouter un champ top-level `schemaVersion` à `GraphModel`.
* *Gate bloquant* : bakery + codebase désérialisent en strict
  (`ObjectMapper().registerKotlinModule()` + `readValue<GraphModel>`,
  `FAIL_ON_UNKNOWN_PROPERTIES = true`). Ajouter le champ *casse* les 2
  consommateurs aujourd'hui.
* Séquence obligatoire :
  . `@JsonIgnoreProperties(ignoreUnknown = true)` chez bakery + codebase
     (S-025 constat #6) — publication + bump de leurs dépendances ;
  . *ensuite* seulement le champ `schemaVersion` dans `GraphModel` (graphify)
     + publication + bump catalogue ;
  . adoption consommateurs (pin catalogue).
* Fichiers concernés : `bakery/lens/SubgraphExtractor.kt:39`,
  `codebase/graph/GraphContext.kt:33`, `codebase/graph/SubgraphExtractor.kt`.
* *NE PAS démarrer sans accord pilote explicite* — c'est un chantier
  cross-borough qui doit être sa propre session.

== Rappel

- Borough N0, tooling 0.0.4 Central (à jour).
- Baby-step DDD/TDD/BDD strict, 3 niveaux (unit → functional → cucumber).
- Filtre `graphify.steps.**` obligatoire sur le task `test`.
- Le cache incrémental vit sous `build/`, jamais committé.
- Version propre dérivée du catalogue — tout bump de version passe par MEMPHIS.
