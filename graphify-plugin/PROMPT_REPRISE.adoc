= PROMPT_REPRISE — Graphify Gradle — Session 030
:session: 030
:date: 2026-09-17

== Session 029 terminée — EPIC D5-RACE (garde de publication racy soldée)

`GraphifyPluginPublicationTest` ne lit plus le working tree du repo voisin
(`../workspace-bom/gradle/libs.versions.toml`, racy entre sessions) : la version
*publiée* du catalogue workspace est *injectée* par Gradle
(`systemProperty("graphify.publishedCatalog.graphifyVersion", ws.versions.graphify.plugin.get())`).

* D5R-0 cadrage — audit 5 constats, 7 décisions. Doc
  `.agents/CADRAGE_EPIC_D5_RACE.adoc`.
* D5R-1 objets purs `graphify.catalog.VersionCatalogToml` (lecture `[versions]`
  *scopée par section*, commentaires) + `graphify.catalog.PublicationHygiene`
  (verdict). 12 tests unit.
* D5R-2 wiring + refactor garde (zéro accès repo voisin). Preuve RED one-shot
  (propriété forcée `9.9.9` → échec).
* D5R-3 BDD `publication_hygiene.feature` 5 scénarios + `PublicationHygieneSteps`.

Gates : 162 unit + 9 functional + 25 cucumber, 0 FAIL. `check --rerun-tasks` +
`publishToMavenLocal` VERTS. Zéro secret, zéro bump, zéro publication Central.

*Les objets d'hygiène vivent dans le source set `test`* — utilitaires de
publication, jamais dans le wire publié.

== COLLISION DE SESSION — à connaître en ouverture

Une *seconde session opencode* détient le borough `bakery-gradle` (et
`workspace-bom`). En S-029 :

* Mission « adoption bakery » (committer le bump 0.0.5 + 2 tests de tolérance)
  *hors scope* — le pilote a interdit de lancer bakery.
* `bakery-gradle` working tree : `graphify = "0.0.5"` + 2 tests de tolérance
  (`SubgraphExtractorTest`) — **non committés** (vérifié verts en 4/4 isolément
  avant consigne d'arrêt).
* Ne PAS travailler dans bakery sans coordination pilote.

== Mission session 030 — à la demande pilote

Aucune urgence. Backlog ouvert :

1. *EPIC PUB* (si accord pilote) — publication Central d'un prochain bump.
   Aujourd'hui 0.0.5 publié ET référencé par `workspace-catalog:0.0.46`.
   Aucun besoin immédiat.
2. *GF-SCHEMA-5* est TERMINÉ ; *GF-FINGERPRINT* TERMINÉ ; *D5-RACE* TERMINÉ.
3. Candidats futurs (non planifiés) : `schemaVersion` DIFFÉRÉ pour
   `graph-content.json` (GF-SCHEMA-5-bis, casserait les mappers stricts
   bakery/codebase du corpus), consolidation de `VersionCatalogToml` vers un
   contrat N0 partagé MEMPHIS (utile à 20+ gardes D5 cross-borough).

== Rappel

- Borough N0, tooling 0.0.5 Central, pin `workspace-catalog:0.0.46`.
- Baby-step DDD/TDD/BDD strict, 3 niveaux (unit → functional → cucumber).
- Filtre `graphify.steps.**` obligatoire sur le task `test`.
- Le source set `functionalTest` n'a PAS `main` sur son classpath de
  compilation — les tests de modèle/domaine vont dans `test/`.
- Version propre dérivée du catalogue : tout bump passe par MEMPHIS.
- La garde de publication exige la propriété
  `graphify.publishedCatalog.graphifyVersion` (injectée par `build.gradle.kts`).
