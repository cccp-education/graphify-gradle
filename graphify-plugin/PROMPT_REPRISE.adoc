= PROMPT_REPRISE — Graphify Gradle — Session 026
:session: 026
:date: 2026-09-17

== Session 025 terminée — EPIC GF-SCHEMA 4/5 US (+ 1 différée)

Déblocage consommateur Brooklin Flow (DFL-2, cross-borough S-216).

* GF-SCHEMA-0 cadrage ✅ — audit 7 constats, doc
  `.agents/CADRAGE_EPIC_GF_SCHEMA.adoc`.
* GF-SCHEMA-1 domaine `graphify.schema` ✅ — `NodeType`/`EdgeType` +
  `GraphSchema` (alias, canonicalisation, vocabulaire UA réservé).
* GF-SCHEMA-2 wiring producteur ✅ — `ScanWorkspaceTask` zéro littéral de type,
  sortie **byte-identique**.
* GF-SCHEMA-3 functional TestKit ✅ — 3 tests (contrat canonique, top-level
  strict-safe, déterminisme).
* GF-SCHEMA-4 BDD Cucumber ✅ — `schema_alias.feature` 7 scénarios, infra
  minimale via convention plugin 0.0.4.
* GF-SCHEMA-5 (`schemaVersion` top-level) ⏳ **DIFFÉRÉ** — casserait bakery +
  codebase (mappers stricts). Exige `@JsonIgnoreProperties` cross-borough.
* README 10 langues synchronisés (section/has_section/subsection).

Gates : 91 unit + 3 functional + 7 cucumber, 0 FAIL. `check` + 
`publishToMavenLocal` VERTS. Zéro secret.

== Prochaine Session (026)

EPIC GF-SCHEMA clos pour sa partie sûre. Le schéma est désormais un domaine
explicite, tolérant et versionné en code (enums), sans casse consommateur.
Brooklin Flow (DSH-BOUNDARY-5) peut consommer `graph.json` canonique.

Priorités possibles (décision pilote) :
1. *GF-SCHEMA-5* (P2) — `schemaVersion` top-level : chantier cross-borough
   (bakery + codebase `@JsonIgnoreProperties(ignoreUnknown = true)` + bump
   dépendances). Ne pas démarrer sans accord pilote explicite.
2. *DFL-5* (P2) — fingerprinting incrémental (optimisation production,
   toujours au backlog Brooklin Flow).
3. *Publication Central 0.0.4* (P0, à la demande) — contenu : GF-SCHEMA.

Zéro initiative : publication/bump uniquement à la demande pilote.

== Rappel

- Borough N0, tooling stable 0.0.3 Central (0.0.4 en attente).
- Baby-step DDD/TDD/BDD strict, 3 niveaux (unit → functional → cucumber).
- *Révision S-024* : l'infra Cucumber est désormais en place (convention
  plugin 0.0.4) — un contrat public le justifie. Ne plus la retirer.
- Filtre `graphify.steps.**` obligatoire sur le task `test` (sinon les
  scénarios Cucumber sont rejoués par l'unit test).
