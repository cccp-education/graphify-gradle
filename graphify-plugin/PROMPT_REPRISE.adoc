= PROMPT_REPRISE — Graphify Gradle — Session 027
:session: 027
:date: 2026-09-17

== Session 026 terminée — EPIC GF-FINGERPRINT TERMINÉ 6/6 US (DFL-5)

Fingerprinting incrémental du scan, deuxième item laissé à graphify (N0) par
le cadrage DSH-BOUNDARY (S-216).

* Domaine `graphify.fingerprint` (7 fichiers, 362 lignes) : `FileFingerprint`,
  `Fingerprinter`, `FileExtraction`/`SectionExtraction`, `ScanCache`,
  `ScanCacheStore` (JSON versionné), `ScanExtractionIndex` (fast path →
  hash de contenu → parse, stats trusted/reused/parsed).
* `FileExtractor` pur — les 5 parsers quittent `ScanWorkspaceTask`.
* Wiring `ScanWorkspaceTask` : `incremental` (opt-in, défaut false) +
  `cacheFile` (défaut `build/graphify/fingerprints.json`), cache élagué,
  *sortie byte-identique* prouvée par les 91 tests historiques.
* Functional TestKit 6 tests, BDD `incremental_scan.feature` 7 scénarios.
* README EN + 9 traductions (plugin + consommateur) synchronisés.
* Doc cadrage : `.agents/CADRAGE_EPIC_GF_FINGERPRINT.adoc`.

Gates : 142 unit (+51) + 9 functional (+6) + 14 cucumber (+7), 0 FAIL.
`check` + `publishToMavenLocal` VERTS. Zéro secret.

== Prochaine Session (027) — Publication Central 0.0.4 (P0, à la demande)

Ordre pilote acté : 2 (fait) → *3* → 1.

* Contenu 0.0.4 : EPIC GF-SCHEMA (S-025) + EPIC GF-FINGERPRINT (S-026).
* Version code : `ws.versions.graphify.plugin` = `0.0.3` → bump `0.0.4`.
* Procédure DAG : `graphify-plugin` n'a aucune dépendance N0 amont.
* Commande : `./gradlew publishAggregationToCentralPortal --no-daemon`
  (nmcp 1.5.0, AUTOMATIC, settings.gradle.kts déjà câblé).
* Vérifier repo1 HTTP 200 sur `education.cccp:graphify-plugin:0.0.4`.
* Puis mettre à jour le catalogue MEMPHIS (`workspace-catalog`) si le pilote
  le demande — ne pas amorcer spontanément un bump cross-borough.

== Puis Session 028 — GF-SCHEMA-5 `schemaVersion` (P2, cross-borough)

Chantier multi-borough, gate = adoption consommateurs. Nécessite
`@JsonIgnoreProperties(ignoreUnknown = true)` chez bakery + codebase avant
d'ajouter le champ top-level `schemaVersion` à `GraphModel`, puis bump des
dépendances. *Ne pas démarrer sans accord pilote explicite.*

== Rappel

- Borough N0, tooling 0.0.3 Central (0.0.4 en cours de session 027).
- Baby-step DDD/TDD/BDD strict, 3 niveaux (unit → functional → cucumber).
- Filtre `graphify.steps.**` obligatoire sur le task `test` (sinon les
  scénarios Cucumber sont rejoués par l'unit test).
- Le cache incrémental vit sous `build/`, jamais committé.
