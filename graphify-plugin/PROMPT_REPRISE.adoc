= PROMPT_REPRISE — Graphify Gradle — Session 025
:session: 025
:date: 2026-09-08

== Session 024 terminée — EPIC GF-CORPUS TERMINÉ 3/3 US

GF-CORPUS-1 audit schéma ✅ (sections + liens TOC non couverts).
GF-CORPUS-3 extension schéma ✅ (TDD strict, 5 baby-steps RED→GREEN :
nœuds `section`, edges `has_section`/`subsection`/`reference toc_entry`,
unicité IDs). GF-CORPUS-2 scan corpus FPA ✅ (dogfooding par configuration :
tâche `scanFpaCorpus` consumer → `office/metiers/FPA/graph-content.json`,
1989 nœuds / 1857 edges / 2.85 MB). Gates : 70/70 PASS (+12), 
publishToMavenLocal VERT. Frontière licence respectée (graphe dans office/).

== Prochaine Session (026)

EPIC GF-CORPUS clos. Le graphe du corpus est disponible pour GBL-003.3
(codex acquisition) et GBL-003.4 (codebase RAG).

Priorité critical path MVP0 — GBL-003 suite : passer la main à Brooklyn
(codex) ou Queens (codebase) selon l'ordonnancement du pilote. Zéro
initiative graphify : le borough retourne en maintenance (liquidé, 0.0.3
Central).

== Rappel

- Borough liquidé (GF-0→6 + PUB + GF-CORPUS terminés), tooling stable 0.0.3 Central.
- Zéro initiative : publication/bump uniquement à la demande pilote.
- Baby-step TDD/BDD : test d'abord, build verify.
- Pas d'infra Cucumber dans ce borough — library pure JUnit5, tests
  unitaires + intégration suffisent (décision S-024).