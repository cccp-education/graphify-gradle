= Test Coverage Analysis — Graphify Gradle

_Statut_ : EPIC A TERMINÉ (GF-2 + GF-3) + GF-CORPUS TERMINÉ (S-024) + GF-SCHEMA 4/5 US (S-025) + GF-FINGERPRINT TERMINÉ (S-026)
_Dernière mise à jour_ : Session 026 (2026-09-17)

== Session 026 — EPIC GF-FINGERPRINT (fingerprinting incrémental), 3 niveaux

- `fingerprint/FileFingerprint.kt` + `Fingerprinter.kt` + `FileExtraction.kt`
  + `ScanCache.kt` + `ScanCacheStore.kt` + `ScanExtractionIndex.kt` : domaine
  pur (nouvelles sources).
- `fingerprint/FileExtractor.kt` : les 5 parsers historiques sortis de
  `ScanWorkspaceTask` (fonction pure `(fileName, content) → FileExtraction`).
- `ScanWorkspaceTask.kt` : options `incremental`/`cacheFile`, `extractEdges`/
  `extractSections` via l'index, parsers privés supprimés. Sortie byte-identique
  (les 91 tests historiques inchangés restent la garde).
- `src/test/kotlin/graphify/fingerprint/FingerprintDomainTest.kt` : 29 unit
  (fast check, sha256, ScanCache record/fast path/hash path/pruning,
  ScanCacheStore round-trip + dégradations).
- `src/test/kotlin/graphify/fingerprint/FileExtractorTest.kt` : 13 unit
  (imports, refs adoc/toc/agent, sections, block delimiters, dispatch, pureté).
- `src/test/kotlin/graphify/fingerprint/ScanExtractionIndexTest.kt` : 9 unit
  (parse unique, fast path sans lecture, hash path, cache ignoré si off,
  updatedCache élagué, stats).
- `src/functionalTest/kotlin/graphify/IncrementalFunctionalTest.kt` : 6 functional
  (TestKit — byte-identité 2e run, contrat canonique sur cache hit, fichier
  modifié/supprimé, cache corrompu, opt-in).
- `src/test/features/incremental_scan.feature` + `graphify/steps/IncrementalScanSteps.kt`
  : 7 scénarios BDD.
- 142 unit (+51) + 9 functional (+6) + 14 cucumber (+7), 0 FAIL.

== Session 025 — EPIC GF-SCHEMA (types + alias), 3 niveaux

- `schema/NodeType.kt` + `schema/EdgeType.kt` + `schema/GraphSchema.kt` : domaine
  pur (nouvelles sources).
- `ScanWorkspaceTask.kt` : littéraux de type remplacés par `NodeType.*.wire` /
  `EdgeType.*.wire` — sortie byte-identique.
- `src/test/kotlin/graphify/schema/GraphSchemaTest.kt` : 15 unit (canonicalisation,
  alias, inconnus préservés, idempotence, vocabulaire réservé).
- `src/test/kotlin/graphify/schema/GraphSchemaModelTest.kt` : 6 unit
  (`canonicalize(model)` entier, préservation des champs, conformance producteur).
- `src/functionalTest/kotlin/graphify/SchemaFunctionalTest.kt` : 3 functional
  (TestKit — contrat canonique, top-level strict-safe, déterminisme).
- `src/test/features/schema_alias.feature` + `graphify/steps/GraphSchemaSteps.kt`
  + `CucumberTestRunner.kt` : 7 scénarios BDD.
- Infra : convention plugins `functional-test` + `cucumber` 0.0.4 ; filtre
  `graphify.steps.**` sur le task `test`.
- 91 unit (+21) + 3 functional + 7 cucumber, 0 FAIL (baseline 70).

== Session 024 — EPIC GF-CORPUS extension schéma TDD strict

- `ScanWorkspaceTask.kt` : +extractSections (nœuds `section`, headingRegex
  excluant délimiteurs de blocs), +buildSubsectionEdges (pile hiérarchique),
  +extractTocTableReferences + resolveTocReference (cellules table + nom),
  unicité IDs `~N`, fix relativisation has_section
- `ScanWorkspaceTaskTest.kt` : +6 tests unitaires (section nodes, block
  delimiters, has_section, subsection hierarchy, toc cells, dedup IDs)
- `ScanWorkspaceIntegrationTest.kt` : +FpaCorpusKnowledgeGraph (end-to-end
  mini-corpus miroir FPA : 7 sections, 4 has_section, 3 subsection, 3 toc)
- Décision : pas de Cucumber (library pure JUnit5, addition de dépendance
  hors périmètre EPIC — économise l'encre)
- 70/70 total PASS (baseline 58 → +12), 0 régression

== Session 013 — Fix @Input validatePlugins

- `VerifyDagAcyclicTask.kt` : foundryDir: File → String (@Input compatible)
- `GraphifyPlugin.kt` : .absolutePath conversion
- `VerifyDagAcyclicTaskTest.kt` : .absolutePath + 14/14 PASS
- `ScanAndVerifyIntegrationTest.kt` : .absolutePath + 3/3 PASS
- 58/58 total PASS, 0 régression

== Couverture actuelle

|===
| Module | Tests | Couverture
| api-key-pool | 66/66 PASS ✅ | migré `foundry/public/api-key-pool-gradle/`
| graphify-plugin | 142 unit + 9 functional + 14 cucumber, 0 FAIL ✅ | voir tableau ci-dessous
|===

Total : 165 PASS (graphify-plugin) + 66 (api-key-pool) ✅

== Tests par composant (graphify-plugin)

|===
| Fichier | Tests | Couvre
| `ScanWorkspaceTaskTest.kt` | 30 unitaires | Exclusions, nœuds (file/dir/project/section), arêtes (contains/import/reference/toc_entry/has_section/subsection), communautés, JSON, empty workspace, .agents/ cross-project, unicité IDs sections
| `ScanWorkspaceIntegrationTest.kt` | 15 intégration | Multi-borough detection, cross-project edges, exclusion verification, output validation, workspace mimic, FpaCorpusKnowledgeGraph (sections+pages+toc end-to-end)
| `VerifyDagAcyclicTaskTest.kt` | 11 unitaires | DAG acyclic OK, N1→N2 violation, N0→N3 violation, suffixes resolution, underscores, custom levels, levelOf()
| `ScanAndVerifyIntegrationTest.kt` | 3 intégration | scan→verify OK, scan→detect violation, engine contract validation
| `GraphModelTest.kt` | 6 | Jackson serialization/deserialization, defaults, NON_NULL labels, GraphCommunity
| `schema/GraphSchemaTest.kt` | 15 unitaires | Canonicalisation nœuds/arêtes, alias pluriels/dash, inconnus préservés, idempotence, vocabulaire UA réservé (S-025)
| `schema/GraphSchemaModelTest.kt` | 6 unitaires | `canonicalize(model)` entier, préservation champs non-type, conformance producteur (S-025)
| `SchemaFunctionalTest.kt` | 3 functional | TestKit end-to-end : contrat canonique, top-level 3 champs strict-safe, déterminisme byte-identique (S-025)
| `schema_alias.feature` + `GraphSchemaSteps.kt` | 7 cucumber | Canonicalisation métier, alias, préservation, idempotence, round-trip legacy (S-025)
| `fingerprint/FingerprintDomainTest.kt` | 29 unitaires | Fast check size/mtime, sha256, ScanCache (record, fast path, hash path, pruning), ScanCacheStore round-trip + dégradations (S-026)
| `fingerprint/FileExtractorTest.kt` | 13 unitaires | Parsers purs : imports, refs adoc/toc/agent, sections, block delimiters, dispatch extension, pureté (S-026)
| `fingerprint/ScanExtractionIndexTest.kt` | 9 unitaires | Mémoïsation par scan : parse unique, fast path sans lecture, hash path, cache ignoré si off, updatedCache élagué, stats (S-026)
| `IncrementalFunctionalTest.kt` | 6 functional | TestKit : byte-identité 2e run, contrat canonique sur cache hit, fichier modifié/supprimé, cache corrompu, opt-in (S-026)
| `incremental_scan.feature` + `IncrementalScanSteps.kt` | 7 cucumber | Cache vide, fast path, drift size/mtime, réutilisation par hash, élagage, round-trip JSON, version inconnue (S-026)
|===

== Scan workspace réel (validation GF-2)

[source]
----
Nodes:        203,204
Edges:        188,365
Communities:  26 (dont les 6 boroughs)
Temps:        4m 51s
Taille:       98 MB
----

== Session 015 — Couverture inchangée

Pas de modification de code source. 58/58 PASS inchangé.

== Session 018 — engine→runner-gradle rename

- `build.gradle.kts` : `"engine" to 3` → `"runner-gradle" to 3`
- `VerifyDagAcyclicTaskTest.kt` : 10 occurrences engine → runner-gradle + com.cheroliv.runner
- `ScanAndVerifyIntegrationTest.kt` : 5 occurrences engine → runner-gradle
- 58/58 PASS inchangé

== Prochaine étape

Session 027 — Publication Maven Central `graphify-plugin` 0.0.4 (contenu :
GF-SCHEMA + GF-FINGERPRINT). Puis GF-SCHEMA-5 (`schemaVersion` top-level) —
différé, exige `@JsonIgnoreProperties` cross-borough bakery/codebase + bump.
